[
  {kernel, [
    {logger_level, debug},
    {logger, [
      {handler, default, logger_std_h, #{formatter => {logger_formatter, #{template => [time, {token, [" [", token, "] "], [" [system] "]}, pid," [", level, "] ",msg,"\n"]}}}}
    ]}
  ]},
  {server, []},
  {client, []}
].
