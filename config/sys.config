[
  {kernel, [
    {logger_level, debug},
    {logger, [
      {handler, default, logger_std_h, #{formatter => {logger_formatter, #{template => [{token, ["[", token, "] "], [" [system] "]}, pid, " ",  mfa, ":", line, " [", level, "] ",msg,"\n"]}}}}
    ]}
  ]},
  {server, []},
  {client, []}
].
